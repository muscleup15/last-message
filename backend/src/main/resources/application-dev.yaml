spring:
  application:
    name: last-message
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/last_message
    username: lastmessage
    password: 12341234

  data:
    redis:
      host: localhost
      port: 6379

  sql:
    init:
      mode: always

app:
  jwt:
    secret: last-message-dev-jwt-secret-key-32b
    expiration-ms: 86400000
  cors:
    allowed-origins: http://localhost:5173
  kakao:
    client-id: ${KAKAO_CLIENT_ID:}
    client-secret: ${KAKAO_CLIENT_SECRET:}
    redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:5173/oauth/kakao}
