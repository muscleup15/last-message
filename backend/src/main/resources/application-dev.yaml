spring:
  application:
    name: last-message
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/last_message
    username: lastmessage
    password: 12341234

  data:
    redis:
      host: localhost
      port: 6379

  sql:
    init:
      mode: always
